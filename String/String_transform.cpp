#include<iostream>
#include<string>
#include<algorithm>

using namespace std;

int main()
{
    // string s ="A";

    // // uppercase
    // transform(s.begin(),s.end(),s.begin(),::toupper);
    // cout<<s<<endl;

    // // lowercase
    // transform(s.begin(),s.end(),s.begin(),::tolower);
    // cout<<s<<endl;


    char ch = 'a';
    ch =ch + 32;
    cout<<ch<<endl;
}